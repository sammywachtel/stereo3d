[project]
name = "stereo3d"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "torch",
    "torchvision",
    "opencv-contrib-python",
    "numpy",
    "pillow",
    "tqdm",
    "timm",
]
